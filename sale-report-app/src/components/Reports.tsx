import React from 'react';
import { OptimizedReports } from './OptimizedReports';

export const Reports: React.FC = () => {
  // Use the optimized reports component for better performance with large datasets
  return <OptimizedReports />;
};
