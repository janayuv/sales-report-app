name: Security Scan

env:
  # Disable Linux-specific system libraries on Windows
  PKG_CONFIG_PATH: ""
  PKG_CONFIG_ALLOW_SYSTEM_CFLAGS: "0"

on:
  push:
    branches: [ main, master, develop ]
  pull_request:
    branches: [ main, master, develop ]
  schedule:
    - cron: '0 2 * * 1' # Weekly on Monday at 2 AM

jobs:
  security-scan:
    name: Security Scan
    runs-on: windows-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: sale-report-app/package-lock.json

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install dependencies
        working-directory: ./sale-report-app
        run: npm ci

      - name: Run npm audit
        working-directory: ./sale-report-app
        run: |
          npm audit --audit-level moderate --json > npm-audit.json || true
          npm audit --audit-level moderate

      - name: Run Rust security audit
        working-directory: ./sale-report-app/src-tauri
        run: |
          cargo install cargo-audit
          cargo audit --json > rust-audit.json || true
          cargo audit

      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'
          scan-ref: '.'
          format: 'sarif'
          output: 'trivy-results.sarif'

      - name: Upload Trivy scan results
        uses: github/codeql-action/upload-sarif@v3
        if: always() && hashFiles('trivy-results.sarif') != ''
        with:
          sarif_file: 'trivy-results.sarif'

      - name: Run CodeQL Analysis
        uses: github/codeql-action/analyze@v3
        with:
          languages: javascript, typescript

      - name: Upload security reports
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: security-reports
          path: |
            sale-report-app/npm-audit.json
            sale-report-app/src-tauri/rust-audit.json
            trivy-results.sarif
          retention-days: 30

  # Dependency review (optional - requires GitHub Advanced Security)
  dependency-review:
    name: Dependency Review
    runs-on: windows-latest
    if: github.event_name == 'pull_request' && github.repository_owner != 'janayuv'
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Dependency Review
        uses: actions/dependency-review-action@v4
        with:
          fail-on-severity: moderate
        continue-on-error: true
